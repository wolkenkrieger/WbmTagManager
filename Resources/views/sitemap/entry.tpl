<url>
	<loc>{$baseurl}/{$manufacturerPath}/{$manufacturerUrl}/{$modelPath}/{$modelUrl}</loc>
{if $lastmod}
	<lastmod>{$lastmod}</lastmod>
{/if}
	<changefreq>monthly</changefreq>
	<priority>{$priority}</priority>
</url>
